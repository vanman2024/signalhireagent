#!/bin/bash

# SignalHire Agent - Fast launcher (no dependency checks)
# Usage: ./sha search --title "Engineer" --location "SF"

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
python3 -m src.cli.main "$@"